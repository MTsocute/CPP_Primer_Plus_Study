//
// Created by Momo on 2023/6/25.
//
#include <iostream>
#include <vector>
using namespace std;

// 创建一个 vector 数组
void create_vector() {
    vector<int> v1;     // 创建一个int的vector 模版名为 v1, 长度为 0
    v1.resize(5);   // 长度增长为 5

    puts("Set the length of v2: ");
    int n;
    cin >> n;
    vector<double> v2(n);   // 创建一个长度为 n 的 vector 模版数组
}

int main() {

    return 0;
}